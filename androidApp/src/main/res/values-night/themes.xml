<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen theme (dark) -->
    <style name="Theme.HelloMeds.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.HelloMeds</item>
    </style>

    <!-- Splash screen colors (dark) — Material You dynamic surface -->
    <color name="splash_background">@android:color/system_neutral1_900</color>

</resources>
